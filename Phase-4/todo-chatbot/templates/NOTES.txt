TaskMaster Pro AI has been deployed successfully!

Components:
  - Backend (FastAPI):  {{ include "taskmaster.backend.fullname" . }}
  - Frontend (Next.js): {{ include "taskmaster.frontend.fullname" . }}

{{- if contains "NodePort" .Values.backend.service.type }}

Access your application:

  Backend API:
    minikube service {{ include "taskmaster.backend.fullname" . }} --url
    or: http://<minikube-ip>:{{ .Values.backend.service.nodePort }}

  Frontend UI:
    minikube service {{ include "taskmaster.frontend.fullname" . }} --url
    or: http://<minikube-ip>:{{ .Values.frontend.service.nodePort }}

  Get Minikube IP:
    minikube ip

{{- else if contains "ClusterIP" .Values.backend.service.type }}

  Port-forward to access locally:

  Backend:
    kubectl port-forward svc/{{ include "taskmaster.backend.fullname" . }} 8000:{{ .Values.backend.service.port }}

  Frontend:
    kubectl port-forward svc/{{ include "taskmaster.frontend.fullname" . }} 3000:{{ .Values.frontend.service.port }}

{{- end }}
