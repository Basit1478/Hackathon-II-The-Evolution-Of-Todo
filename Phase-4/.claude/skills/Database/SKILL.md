# Database Development

SQLModel, Neon Serverless PostgreSQL, Alembic migrations
Create Task, Conversation, Message models with relationships
User isolation patterns, indexes, foreign keys, auto-timestamps
