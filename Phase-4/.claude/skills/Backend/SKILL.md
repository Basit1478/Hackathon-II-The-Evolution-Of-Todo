# Backend Development

FastAPI, OpenAI Agents SDK, Official MCP SDK, SQLModel
Build stateless chat API, integrate MCP tools, persist conversations
JWT verification, user isolation, stateless architecture patterns
