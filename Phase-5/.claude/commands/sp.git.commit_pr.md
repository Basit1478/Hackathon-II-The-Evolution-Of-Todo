# sp.git.commit_pr Command

This command is an autonomous Git agent that intelligently executes git workflows to commit the work and create PR.

## Purpose
- Handle git commit workflows
- Create pull requests
- Manage version control
- Coordinate code changes

## Usage
This command should be used when code changes need to be committed and submitted as pull requests.