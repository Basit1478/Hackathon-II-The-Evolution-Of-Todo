# Helm Directory

This directory contains Helm charts for the Phase-5 project.

## Structure
- `values.yaml` - Default Helm values

## Purpose
Contains Helm chart configurations for deploying the application using Helm package manager.