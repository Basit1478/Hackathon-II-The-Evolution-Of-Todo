apiVersion: v1
kind: Service
metadata:
  name: {{ include "taskmaster.backend.fullname" . }}
  labels:
    {{- include "taskmaster.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      {{- if and (eq .Values.backend.service.type "NodePort") .Values.backend.service.nodePort }}
      nodePort: {{ .Values.backend.service.nodePort }}
      {{- end }}
      protocol: TCP
      name: http
  selector:
    {{- include "taskmaster.backend.selectorLabels" . | nindent 4 }}
