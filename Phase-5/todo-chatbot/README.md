# Todo Chatbot Directory

This directory contains the Helm chart for the Todo Chatbot.

## Structure
- `templates/` - Helm chart templates
- `values.yaml` - Default Helm values

## Purpose
Contains Helm chart configuration for deploying the Todo Chatbot application.